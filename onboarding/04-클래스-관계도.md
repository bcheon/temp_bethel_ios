# 04. 클래스 관계도

## iOS Swift 클래스 — 책임 분리

iOS 앱은 **싱글톤 서비스 + ObservableObject + SwiftUI Views** 구조입니다.

```mermaid
classDiagram
    class BKCApp {
        +AppDelegate appDelegate
        +init()
    }
    class AppDelegate {
        +didFinishLaunching()
        +didRegisterForRemoteNotifications()
        +continue userActivity (Universal Link)
        +messaging(didReceiveRegistrationToken)
        +willPresent (foreground notif)
        +didReceive (notif tapped)
    }

    class PushService {
        <<singleton>>
        -keychainService
        +requestAuthorization() async Bool
        +getOrCreateDeviceID() String
        +subscribe(toGroup) async throws
        +unsubscribe(fromGroup) async throws
        +handleTokenRefresh(token)
    }

    class GroupStore {
        <<singleton, ObservableObject>>
        +groups Set~String~
        +hasOnboarded Bool
        +completeOnboarding(groups) async throws
        +setGroups(newGroups) async throws
        +resyncFromServer() async throws
    }

    class GroupSync {
        <<static>>
        +apply(toAdd, toRemove, ops) async throws
    }
    class GroupSyncOps {
        <<struct>>
        +subscribe (closure)
        +unsubscribe (closure)
    }

    class CampaignCache {
        <<singleton, ObservableObject>>
        +campaigns [PushCampaign]
        +lastSyncedAt Date?
        +merge(deltas)
        +markRead(uuid)
        +clear() (test seam)
    }

    class BKCAPIClient {
        <<singleton, BKCAPI>>
        -session URLSession
        -baseURL URL
        +subscribe(token, deviceID, groups) async throws
        +updateGroups(deviceID, groups) async throws
        +unsubscribe(deviceID) async throws
        +fetchCampaigns(since) async throws
        +sendEvents(events) async throws Int
    }
    class BKCAPI {
        <<protocol>>
    }

    class DeepLinkRouter {
        <<singleton, ObservableObject>>
        +selectedTab Int
        +injectedHomeURL URL?
        +highlightedCampaignUUID String?
        +route(url)
    }

    class TelemetryService {
        <<singleton>>
        -pendingKey "bkc.telemetry.pending"
        -flushTimer Timer (5min)
        +logDelivered(uuid)
        +logOpened(uuid)
        +logDeeplinked(uuid)
        +flush() async
    }

    class RootTabView
    class WebViewTab
    class WebViewStore
    class NotificationListView
    class GroupOnboardingView
    class MoreView

    class SubscriptionGroup {
        <<enum>>
        all, youth, newfamily
        +topic String
        +displayName String
        +isMandatory Bool
    }
    class PushCampaign {
        <<struct, Codable>>
        uuid, title, body, deepLink, sentAt, isRead
    }
    class DeepLink {
        <<enum>>
        home, sermon, news, event, external
        +from(url) DeepLink
    }
    class TelemetryEvent {
        <<struct, Codable>>
        campaignUUID, deviceID, eventType, occurredAt
    }

    BKCApp --> AppDelegate
    BKCApp --> RootTabView
    AppDelegate --> PushService
    AppDelegate --> TelemetryService
    AppDelegate --> DeepLinkRouter
    AppDelegate ..> SubscriptionGroup
    PushService ..> SubscriptionGroup
    GroupStore --> PushService
    GroupStore --> BKCAPIClient
    GroupStore --> GroupSync
    GroupStore --> CampaignCache
    GroupSync --> GroupSyncOps
    CampaignCache --> PushCampaign
    BKCAPIClient ..|> BKCAPI
    BKCAPIClient --> PushCampaign
    BKCAPIClient --> TelemetryEvent
    DeepLinkRouter --> DeepLink
    DeepLinkRouter --> TelemetryService
    TelemetryService --> BKCAPIClient
    TelemetryService --> PushService
    TelemetryService --> TelemetryEvent

    RootTabView --> WebViewTab
    RootTabView --> NotificationListView
    RootTabView --> MoreView
    RootTabView --> DeepLinkRouter
    WebViewTab --> WebViewStore
    NotificationListView --> CampaignCache
    NotificationListView --> BKCAPIClient
    NotificationListView --> DeepLinkRouter
    GroupOnboardingView --> GroupStore
    MoreView --> GroupStore
```

### iOS 핵심 클래스 책임 한 줄 요약

| 클래스 | 한 줄 |
|--------|------|
| `BKCApp` | `@main` 진입점. Firebase 초기화, AppDelegate 부착. |
| `AppDelegate` | iOS 시스템 콜백 허브. APNs 토큰, FCM 토큰, 알림 탭, Universal Link. |
| `PushService` | FCM 토픽 구독/해지 + Keychain 디바이스 ID. **외부 SDK와의 유일한 접점.** |
| `GroupStore` | "사용자가 어떤 그룹을 구독 중"이라는 진실의 단일 출처. UI가 관찰. 변경 시 FCM + 서버 양쪽 동기화 + 롤백. |
| `GroupSync` | rollback 로직만 담당하는 순수 함수. 테스트가 mock ops 주입 가능. |
| `CampaignCache` | 받은 공지 메모리/디스크 캐시 (최대 50개, FIFO). |
| `BKCAPIClient` | WordPress REST API 호출 래퍼. 5xx 1회 재시도. |
| `DeepLinkRouter` | URL → 탭 전환 / WebView injection / external Safari 분기. |
| `TelemetryService` | delivered/opened/deeplinked 이벤트 버퍼링 + 5분 주기 flush. |
| `SubscriptionGroup` | 그룹 enum + FCM 토픽 매핑 (`youth → bkc_youth`). |
| `DeepLink` | URL 분류 enum. **WebView 안에서 안 열려야 할 외부 URL 판정.** |

### iOS 의존성 주입 / 테스트 가능성 패턴

세 가지 패턴이 반복됩니다:

1. **`URLSession` 주입** (`BKCAPIClient(session:)`) — 테스트가 `MockURLProtocol` 주입.
2. **Ops struct 추출** (`GroupSyncOps`) — `GroupSync.apply`가 `PushService` 직접 의존 안 함, closure 받음.
3. **`clear()` 테스트 seam** (`CampaignCache.clear`, `TelemetryService.loadPendingForTesting`) — 싱글톤 잔여 상태 리셋용 (production 호출 금지).

---

## PHP (WordPress 플러그인) 클래스 관계도

PHP 측은 **static 헬퍼 클래스 + WP 훅** 구조입니다 (PSR-4 namespace 안 씀, `BKC_` prefix).

```mermaid
classDiagram
    class bkc_push_php {
        <<entry>>
        register_activation_hook
        register_deactivation_hook
        add_action(rest_api_init)
        add_action(admin_menu)
        add_action(bkc_dispatch_campaign)
        add_action(bkc_stats_rollup)
        bkc_push_run_migrations()
    }

    class BKC_REST_API {
        <<static>>
        +NAMESPACE "bkc/v1"
        +register_routes()
        +require_admin() permission_callback
        +handle_subscribe()
        +handle_update_subscription()
        +handle_delete_subscription()
        +handle_list_campaigns()
        +handle_events()
        +handle_campaign_stats() admin
        +handle_subscriber_stats() admin
        +handle_cancel_campaign() admin
    }

    class BKC_Admin {
        <<static>>
        +add_menu()
        +enqueue_scripts()
        +render_dashboard()
        +render_compose() handles POST
        +render_campaigns()
    }

    class BKC_Groups {
        <<static>>
        +WHITELIST = [all, youth, newfamily]
        +TOPIC_MAP
        +is_valid(group) bool
        +to_topic(group) string
        +validate_array(groups) bool
    }

    class BKC_Subscriptions {
        <<static>>
        +upsert(token, deviceID, platform, ver, groups)
        +update_groups(deviceID, groups) bool
        +delete(deviceID) bool
        +prune_stale(days) int
        +count_targeted(groups) int
    }

    class BKC_Campaigns {
        <<static>>
        +create(title, body, deeplink, groups, userID)
        +transition_status(uuid, from, to, extra) bool
        +find_by_uuid(uuid) object?
        +list(limit, offset, statusFilter) array
    }

    class BKC_Events {
        <<static>>
        +VALID_EVENT_TYPES = [delivered, opened, deeplinked]
        +record_batch(events) int
        +prune_older_than(months) int
    }

    class BKC_Rate_Limiter {
        <<static>>
        +check(action, ip, limit, windowSec) bool
        +get_client_ip() string
    }

    class BKC_Dispatcher {
        <<static>>
        +enqueue(uuid) actionID
        +dispatch_handler(args)
        +cancel(uuid) bool
    }

    class BKC_FCM_Client {
        -service_account
        -project_id
        +__construct() loads service account JSON
        +get_access_token() string
        +send_to_groups(title, body, deeplink, groups, uuid)
        +is_unregistered_error(response) bool static
        -build_jwt(now) string
    }

    class BKC_Stats_Rollup {
        <<static>>
        +LAST_PRUNE_OPTION
        +register_cron()
        +rollup_handler()
        -rollup_single(uuid)
        -maybe_prune_events()
    }

    class WPDB {
        <<external>>
        wp_bkc_campaigns
        wp_bkc_subscriptions
        wp_bkc_campaign_events
        wp_bkc_campaign_stats
    }
    class ActionScheduler {
        <<external>>
        as_schedule_single_action
        as_schedule_recurring_action
        as_unschedule_action
    }
    class FCM_HTTP {
        <<external>>
        oauth2.googleapis.com/token
        fcm.googleapis.com/v1/projects/.../messages:send
    }

    bkc_push_php --> BKC_REST_API : registers
    bkc_push_php --> BKC_Admin : registers
    bkc_push_php --> BKC_Dispatcher : action handler
    bkc_push_php --> BKC_Stats_Rollup : action handler
    bkc_push_php --> WPDB : dbDelta migrations

    BKC_REST_API --> BKC_Rate_Limiter
    BKC_REST_API --> BKC_Groups
    BKC_REST_API --> BKC_Subscriptions
    BKC_REST_API --> BKC_Campaigns
    BKC_REST_API --> BKC_Events
    BKC_REST_API --> BKC_Dispatcher

    BKC_Admin --> BKC_Campaigns
    BKC_Admin --> BKC_Groups
    BKC_Admin --> BKC_Dispatcher

    BKC_Dispatcher --> BKC_Campaigns
    BKC_Dispatcher --> BKC_Subscriptions
    BKC_Dispatcher --> BKC_FCM_Client
    BKC_Dispatcher --> ActionScheduler

    BKC_Stats_Rollup --> BKC_Events
    BKC_Stats_Rollup --> ActionScheduler
    BKC_Stats_Rollup --> WPDB

    BKC_FCM_Client --> BKC_Groups
    BKC_FCM_Client --> FCM_HTTP

    BKC_Groups --> WPDB : (none)
    BKC_Subscriptions --> WPDB
    BKC_Campaigns --> WPDB
    BKC_Events --> WPDB
```

### PHP 핵심 클래스 책임 한 줄 요약

| 클래스 | 한 줄 |
|--------|------|
| `bkc-push.php` | 플러그인 진입점. 클래스 로딩, 훅 등록, DB 마이그레이션. |
| `BKC_REST_API` | 8개 REST 엔드포인트 등록 + 핸들러. 입력 sanitize + 권한 체크. |
| `BKC_Admin` | WP 어드민 메뉴 + compose form POST 처리 + idempotency. |
| `BKC_Groups` | 그룹 화이트리스트 + FCM 토픽 매핑. **단일 출처.** |
| `BKC_Subscriptions` | `wp_bkc_subscriptions` CRUD + 활성 구독자 count + 14일 prune. |
| `BKC_Campaigns` | `wp_bkc_campaigns` CRUD + **원자적 status 전환** (`WHERE status=$from`). |
| `BKC_Events` | raw 이벤트 batch insert. UUID/timestamp 검증. INSERT IGNORE 로 dedup. |
| `BKC_Rate_Limiter` | per-IP per-action transient 카운터. |
| `BKC_Dispatcher` | Action Scheduler hook. queued→sending→sent/failed 전환 오케스트레이션. |
| `BKC_FCM_Client` | JWT 서명 + OAuth2 + FCM HTTP v1 호출. **외부 HTTP의 유일한 접점.** |
| `BKC_Stats_Rollup` | 매시간 cron. raw event → 캠페인별 집계. **멱등(IRON RULE #7).** |

### PHP 측 의존성 방향

화살표가 한 방향만 흐릅니다 (순환 의존성 없음):

```
Entry (bkc-push.php)
  ↓
Routing (REST_API, Admin)
  ↓
Domain (Campaigns, Subscriptions, Events, Groups, Rate_Limiter)
  ↓
Infrastructure (Dispatcher, FCM_Client, Stats_Rollup)
  ↓
External (WPDB, ActionScheduler, FCM HTTP)
```

---

## 양쪽을 잇는 계약 (Contract)

iOS와 PHP가 모두 알아야 하는 두 가지 계약:

### 1. 그룹 식별자 (양쪽이 똑같아야 함)

| Swift `SubscriptionGroup` | PHP `BKC_Groups::WHITELIST` | FCM 토픽 |
|---------------------------|----------------------------|---------|
| `.all` ("all") | `'all'` | `bkc_all` |
| `.youth` ("youth") | `'youth'` | `bkc_youth` |
| `.newfamily` ("newfamily") | `'newfamily'` | `bkc_newfam` |

> 새 그룹 추가 시 **양쪽 동시 업데이트 필수** (CLAUDE.md "보안 룰 #5"). 한쪽만 바꾸면 구독 등록은 되는데 발송이 안 되는 / 그 반대 상황 발생.

### 2. JSON 페이로드 (스네이크/카멜 변환)

iOS는 `JSONDecoder.keyDecodingStrategy = .convertFromSnakeCase` 사용. 즉:
- 서버: `campaign_uuid`, `device_id`, `target_groups`, `sent_at`
- Swift: `campaignUUID`, `deviceID`, `targetGroups`, `sentAt`

이벤트 타입은 변환되지 않으므로 양쪽 모두 lower snake case:
- `delivered`, `opened`, `deeplinked`

## 다음에 읽기

- 위 클래스가 다루는 DB 컬럼 / JSON 형식 → [`05-데이터-모델.md`](05-데이터-모델.md)
- 새 개발자가 처음 코드를 만지기 전에 → [`06-개발환경-셋업.md`](06-개발환경-셋업.md)
