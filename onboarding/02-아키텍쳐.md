# 02. 아키텍쳐

## 컴포넌트 다이어그램

```mermaid
flowchart TB
    subgraph 클라이언트["📱 iOS 클라이언트 (이 리포: ios/BKC/)"]
        direction TB
        UI[SwiftUI Views<br/>RootTabView · NotificationListView<br/>GroupOnboardingView · MoreView · WebViewTab]
        SVC[Services<br/>PushService · GroupStore · CampaignCache<br/>BKCAPIClient · DeepLinkRouter · TelemetryService]
        APPDEL[AppDelegate<br/>UNUserNotificationCenterDelegate<br/>MessagingDelegate]
        NSE[NotificationServiceExtension<br/>delivered 이벤트 기록]
        UI <--> SVC
        APPDEL --> SVC
    end

    subgraph 서버["🖥 WordPress 서버 (이 리포: wordpress-plugin/bkc-push/)"]
        direction TB
        REST[BKC_REST_API<br/>/subscribe /campaigns /events /stats]
        ADMIN[BKC_Admin<br/>대시보드 · 작성폼 · 발송이력]
        DOMAIN[Domain Classes<br/>Groups · Campaigns · Subscriptions<br/>Events · RateLimiter]
        FCMC[BKC_FCM_Client<br/>JWT 서명 · OAuth2 토큰 · HTTP v1]
        DISP[BKC_Dispatcher<br/>Action Scheduler 핸들러]
        ROLL[BKC_Stats_Rollup<br/>매시간 집계]
        DB[(MySQL<br/>4 테이블)]
        REST --> DOMAIN
        ADMIN --> DOMAIN
        DOMAIN --> DB
        ADMIN --> DISP
        DISP --> FCMC
        DISP --> DOMAIN
        ROLL --> DB
    end

    subgraph 외부["☁️ 외부 서비스"]
        FCM[Firebase Cloud Messaging<br/>HTTP v1 API]
        APNS[Apple APNs]
        APPSTORE[App Store / TestFlight]
    end

    SVC -->|HTTPS REST| REST
    NSE -->|HTTPS POST /events| REST
    APPDEL -->|토큰 등록| FCM
    SVC -->|토픽 구독/해지| FCM
    FCMC -->|푸쉬 발송| FCM
    FCM --> APNS
    APNS --> APPDEL
    APPSTORE -.설치.-> 클라이언트
```

## 배포 토폴로지

```mermaid
flowchart LR
    subgraph dev["로컬 개발자"]
        XCODE[Xcode 16+<br/>시뮬레이터]
        DOCKER[Docker + act<br/>CI 시뮬레이션]
    end

    subgraph github["GitHub"]
        REPO[bcheon/temp_bethel_ios<br/>main 브랜치]
        CI[GitHub Actions<br/>actionlint · wp-test · ios-test]
    end

    subgraph apple["Apple 인프라"]
        APPCONNECT[App Store Connect]
        TESTFLIGHT[TestFlight<br/>내부 테스터]
        STORE[App Store<br/>Production]
        APNS_PROD[APNs Production]
        APNS_SANDBOX[APNs Sandbox]
    end

    subgraph google["Google 인프라"]
        FCM_PROJ[Firebase Project<br/>bkc-prod]
        FCM_API[FCM HTTP v1]
    end

    subgraph bkc["bkc.org 인프라"]
        WP_STAGE[Staging WP<br/>stage.bkc.org]
        WP_PROD[Production WP<br/>bkc.org]
        SECRETS[`/var/www/bkc-secrets/<br/>chmod 600`]
        AASA[`/.well-known/<br/>apple-app-site-association`]
    end

    XCODE -->|build/test| github
    DOCKER -->|act| github
    REPO --> CI
    CI -->|fastlane beta| TESTFLIGHT
    TESTFLIGHT --> STORE
    STORE -.설치.-> APNS_PROD
    XCODE -.시뮬레이터.-> APNS_SANDBOX
    CI -->|wp-cli| WP_STAGE
    WP_STAGE -.수동승격.-> WP_PROD
    WP_PROD --> SECRETS
    WP_PROD --> AASA
    WP_PROD <--> FCM_API
    FCM_API --> APNS_PROD
```

## 데이터 흐름 — Push 발송 전체 경로

```mermaid
flowchart LR
    A[관리자 웹브라우저] -->|POST 폼| B[WP 어드민 nonce 검증]
    B -->|insert + enqueue| C[(wp_bkc_campaigns<br/>status=queued)]
    B --> D[Action Scheduler<br/>10초 지연]
    D -->|bkc_dispatch_campaign| E[BKC_Dispatcher::dispatch_handler]
    E -->|atomic transition| C
    E -->|count_targeted| F[(wp_bkc_subscriptions)]
    E -->|init stats| G[(wp_bkc_campaign_stats)]
    E --> H[BKC_FCM_Client::send_to_groups]
    H -->|JWT + OAuth2| I[Google OAuth2 token endpoint]
    I -->|access_token| H
    H -->|HTTPS POST messages:send| J[FCM HTTP v1]
    J --> K[APNs]
    K -->|APNs push| L[교인 iPhone OS]
    L -->|wake NSE| M[NotificationServiceExtension]
    M -->|POST /events delivered| N["/wp-json/bkc/v1/events/"]
    L -->|시스템 알림 표시| O[사용자]
    O -->|tap| P[App 열림 + UNUserNotificationCenterDelegate]
    P -->|opened + deeplinked| Q[TelemetryService 버퍼]
    Q -->|5분 주기 flush| N
    N -->|INSERT IGNORE| R[(wp_bkc_campaign_events)]
    S[Action Scheduler<br/>매시간] --> T[BKC_Stats_Rollup::rollup_handler]
    T -->|COUNT DISTINCT| R
    T -->|UPSERT| G
```

## 신뢰성 메커니즘

| 메커니즘 | 위치 | 보호 대상 |
|---------|------|----------|
| **Idempotency token** | `compose.php` form + `BKC_Campaigns::find_by_uuid` | 더블 submit 시 캠페인 1개만 생성 |
| **Atomic state transition** | `BKC_Campaigns::transition_status` (`WHERE status=$from`) | 동시 dispatcher가 같은 캠페인 두 번 발송 안 함 |
| **Action Scheduler 재시도** | WP 표준 메커니즘 | 일시적 장애 시 자동 재시도 |
| **OAuth2 토큰 캐싱** | `BKC_FCM_Client` (50분 transient) | 매번 JWT 서명 안 해서 부담 적음 |
| **Group sync rollback** | iOS `GroupSync.apply` | FCM 부분 실패 시 모든 변경 롤백 |
| **Rate limiting** | `BKC_Rate_Limiter` (per-IP transient) | 같은 IP의 폭주 차단 |
| **Telemetry offline buffer** | iOS `TelemetryService` (UserDefaults) | 네트워크 끊겨도 7일간 버퍼링 |
| **NSE 5초 타임 budget** | `NotificationService.didReceive` (DispatchGroup wait 5s) | 네트워크 느려도 알림 표시는 보장 |
| **Server-side dedup** | `INSERT IGNORE` + UNIQUE key on (device, campaign, event_type) | 같은 이벤트 중복 카운트 안 됨 |
| **Stats rollup 멱등** | `BKC_Stats_Rollup::rollup_single` (COUNT DISTINCT 재계산) | 두 번 돌려도 같은 숫자 |

## 보안 경계

```mermaid
flowchart TB
    subgraph Public["🌐 Public Internet"]
        BROWSER[교인 브라우저/앱]
    end
    subgraph DMZ["WordPress Public Endpoints"]
        SUB[POST /subscribe<br/>rate-limit 10/min/IP]
        CAMP[GET /campaigns<br/>rate-limit 10/min/IP]
        EVT[POST /events<br/>rate-limit 10/min/IP, max 100건/배치]
    end
    subgraph Admin["WP Admin (manage_options)"]
        STATS[GET /stats/*]
        CANCEL[POST /campaigns/uuid/cancel]
        COMPOSE[POST 작성폼 + nonce]
    end
    subgraph Secret["🔒 웹루트 바깥"]
        SVCKEY[`/var/www/bkc-secrets/<br/>fcm-service-account.json<br/>chmod 600`]
    end

    BROWSER --> SUB
    BROWSER --> CAMP
    BROWSER --> EVT
    Admin -->|read-only| SVCKEY
```

핵심 룰:

- 공개 엔드포인트는 **rate-limit + 입력 sanitize 만**. 인증 안 함 (디바이스 UUID는 비밀이 아님).
- 관리자 엔드포인트는 **`current_user_can('manage_options')` 가드 필수**.
- 발송 키(`fcm-service-account.json`)는 **웹 루트 바깥** + chmod 600 + WP 프로세스만 읽기.
- iOS 앱에는 **자격 증명 없음**. 토큰만 가짐. 누가 토큰을 훔쳐도 푸쉬 발송은 못 함.

## 라이프사이클 상태

### 캠페인 상태 머신

```mermaid
stateDiagram-v2
    [*] --> draft: 작성 중 (사용 안 함, 향후 확장)
    draft --> queued: 발송 등록
    [*] --> queued: 새 공지 작성 폼 submit
    queued --> sending: dispatcher 클레임
    queued --> cancelled: 관리자 취소
    sending --> sent: FCM 응답 200 + message_id
    sending --> failed: FCM 오류 / 예외
    failed --> [*]
    sent --> [*]
    cancelled --> [*]
```

### 디바이스 구독 라이프사이클

```mermaid
stateDiagram-v2
    [*] --> Fresh: 앱 첫 실행
    Fresh --> Onboarding: GroupOnboardingView 표시
    Onboarding --> Subscribed: completeOnboarding 성공
    Onboarding --> Fresh: 실패 시 롤백 (UserDefaults 변경 안 함)
    Subscribed --> Subscribed: 토큰 갱신 (handleTokenRefresh)
    Subscribed --> Subscribed: 그룹 변경 (setGroups)
    Subscribed --> Pruned: 14일간 last_seen 갱신 없으면 prune_stale
    Pruned --> [*]
```

## 주요 파일 위치 요약

| 책임 | 파일 |
|------|------|
| iOS 진입점 | `ios/BKC/BKC/App/BKCApp.swift`, `AppDelegate.swift` |
| iOS 푸쉬 | `ios/BKC/BKC/Services/PushService.swift` |
| iOS 그룹 관리 | `ios/BKC/BKC/Services/GroupStore.swift` |
| iOS API 클라이언트 | `ios/BKC/BKC/Services/BKCAPIClient.swift` |
| iOS NSE | `ios/BKC/BKCNotificationServiceExtension/NotificationService.swift` |
| WP 진입점 | `wordpress-plugin/bkc-push/bkc-push.php` |
| WP REST | `wordpress-plugin/bkc-push/includes/class-bkc-rest-api.php` |
| WP 발송 | `wordpress-plugin/bkc-push/includes/class-bkc-dispatcher.php` |
| WP FCM 클라이언트 | `wordpress-plugin/bkc-push/includes/class-bkc-fcm-client.php` |
| WP 통계 집계 | `wordpress-plugin/bkc-push/includes/class-bkc-stats-rollup.php` |
| WP 어드민 UI | `wordpress-plugin/bkc-push/admin/class-bkc-admin.php` |
| AASA | `well-known/apple-app-site-association` |
| CI | `.github/workflows/ci.yml` |

## 다음에 읽기

- 위 다이어그램의 각 화살표를 시간순으로 펼친 것 → [`03-시퀀스-다이어그램.md`](03-시퀀스-다이어그램.md)
- 클래스끼리 누가 누구를 호출하는지 → [`04-클래스-관계도.md`](04-클래스-관계도.md)
